plugins {
    id "org.sonarqube" version "3.3"
}

sonarqube {
    properties {
        property "sonar.projectKey", "html-css"
        property "sonar.host.url", "http://<SQ-SERVER-IP>:9000"
        property "sonar.login", "sq-token"
    }
}
